
<div class="container">
  <div class="row">
    <h1>All recipes</h1>
    <p>Testing</p>
  </div>
  <div class="recipe-cards">
    <% @recipes.each do |recipe| %>
      <div class="card-category" style='background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= asset_path "#{recipe.image_url}" %>)'>
      </div>
    <% end %>
  </div>

</div>


    <%# <!--
      <div class="card-recipe">
          <%= link_to image_tag(recipe.image_url, alt: "#{recipe.title} cover art", class: "card-recipe-img"), recipe_path(@recipes) %>

        <%# <div class="card-recipe-infos">
          <h2><%="#{recipe.title}"</h2>%>
        <%# </div>
      </div>  --> %>
